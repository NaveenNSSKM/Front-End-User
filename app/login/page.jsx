import LoginForm from '@/components/auth/LoginForm';

export const metadata = {
  title: 'Login - AuthDash',
  description: 'Sign in to your AuthDash account',
};

export default function LoginPage() {
  return <LoginForm />;
}
