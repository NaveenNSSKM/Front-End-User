import RegisterForm from '@/components/auth/RegisterForm';

export const metadata = {
  title: 'Register - AuthDash',
  description: 'Create your AuthDash account',
};

export default function RegisterPage() {
  return <RegisterForm />;
}
